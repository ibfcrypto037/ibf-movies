// Mock data has been removed — all data is now fetched from Supabase.
export const mockMovies: never[] = [];
export const mockRequests: never[] = [];
export const mockLanguages: never[] = [];
export const mockSettings = { custom_message: '', use_custom: false };
